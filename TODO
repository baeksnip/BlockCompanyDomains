# Methodology to find out the domains of a device
-Off and on
-Utilization
-Execution of main functions

# Methodology to find out the domains of a service
-Use

# Prevent the use of other dns resolvers that a device uses in its own way outside of being able to configure it.
